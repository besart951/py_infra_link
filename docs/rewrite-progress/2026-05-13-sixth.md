# Daily Rewrite Progress - 2026-05-13

## Summary

Added HTTP-level integration tests for JWT auth flow and protected project route ownership checks using FastAPI app routes against the real PostgreSQL test container setup.

## Repository State

- Backend architecture and domain modules remain stable.
- Existing validation baseline was green before changes (218 tests).
- After this run: 222 tests pass.

## Domain / Module Selected

Cross-domain consistency and cleanup (priority 15): HTTP presentation-layer integration coverage for `auth` and `project` modules.

## Why This Candidate

`STATUS.md` identified HTTP-level route tests (with JWT auth flow against real Postgres) as the highest-leverage next step after repository-level integration adapter coverage was complete.

## Dependency Classification

Local-substitutable.

- Real PostgreSQL (testcontainers) and real Alembic migrations are used.
- HTTP routes execute in-process through ASGI transport.

## Seam Decision

No new Seam introduced.

- Reused existing FastAPI dependency seam (`get_session`) via test-time dependency override.
- Reused existing JWT and route ownership enforcement Interfaces.

## Interface Chosen

FastAPI route Interfaces:

- `POST /auth/register`
- `POST /auth/login`
- `POST /users/{owner_id}/projects`
- `GET /users/{owner_id}/projects`

## Implementation Changes

- Added `backend/app/integration_tests/test_http_auth_project_routes.py`.
- Added an async HTTP test fixture that:
  - Injects integration `AsyncSession` via `app.dependency_overrides[get_session]`.
  - Wires `ConnectionManager` onto app state for route dependencies.
  - Uses `httpx.AsyncClient` + `ASGITransport` for route-level calls.
- Added behavior tests for:
  - Register returns bearer token with valid JWT subject claim.
  - Login returns bearer token for valid credentials.
  - Protected project route rejects missing auth token.
  - Project routes allow owner token and deny different user token.

## Tests Added or Changed

Added 4 integration tests:

- `test_register_returns_bearer_token_with_subject_claim`
- `test_login_returns_new_token_for_existing_credentials`
- `test_create_project_requires_bearer_token`
- `test_project_routes_allow_owner_and_forbid_different_user`

## Migrations Added or Changed

None.

## Validation Results

From `backend/`:

- `uv run --extra dev ruff check app/` ✅
- `uv run --extra dev pyright` ✅
- `uv run --extra dev pytest` ✅ (222 passed)

## Deleted or Replaced Shallow Modules

None.

## Remaining Risks

- JWT secret currently defaults to a short development key and produces runtime cryptographic-strength warnings in tests.
- HTTP-level integration coverage is still partial (focus currently on auth + project routes).

## Next Suggested Domain

Continue cross-domain consistency:

- Expand HTTP-level integration tests to additional protected route modules (notifications and project-resource linking), including unauthorized and ownership error modes.

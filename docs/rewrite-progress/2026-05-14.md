# Daily Rewrite Progress - 2026-05-14

## Summary

Continued route-level integration hardening by adding HTTP integration tests for notification and project-resource-link endpoints, and enforcing ownership-based auth on project-resource-link routes.

## Repository State

- Baseline validation was green before changes.
- Existing coverage already included auth/project HTTP route tests.
- Post-change suite is green with 225 passing tests.

## Domain / Module Selected

Cross-domain consistency and cleanup: protected presentation Interfaces in `notification` and `project_resource_link` modules.

## Why This Candidate

`STATUS.md` recommended continuing HTTP-level route integration hardening for notification and project-resource-link endpoints (auth + ownership + error modes).

## Dependency Classification

Local-substitutable.

- Real PostgreSQL integration fixture via testcontainers.
- In-process HTTP route execution via `httpx.AsyncClient` + `ASGITransport`.

## Seam Decision

No new Seam introduced.

- Reused existing `get_current_user` auth dependency seam.
- Reused existing per-request session seam (`get_session`) via integration override.

## Interface Chosen

- `GET /users/{user_id}/notifications`
- `PATCH /users/{user_id}/notifications/{notification_id}/read`
- `POST /users/{owner_id}/projects/{project_id}/links`
- `GET /users/{owner_id}/projects/{project_id}/links`
- `DELETE /users/{owner_id}/projects/{project_id}/links/{link_id}`

## Implementation Changes

- Added ownership auth guard to all project-resource-link route handlers:
  - Injected `current_user: User = Depends(get_current_user)`.
  - Added `_require_owner(current_user, owner_id)` with `403` for non-owners.
- Added HTTP integration test file:
  - `backend/app/integration_tests/test_http_notification_project_resource_link_routes.py`.
- New tests cover:
  - Missing token (`401`) on protected routes.
  - Cross-user access denied (`403`).
  - Owner success flows (`200`/`201`).
  - Not-found error modes (`404`) for notification/read and link/delete paths.

## Tests Added or Changed

Added 3 integration tests:

- `test_notification_routes_require_auth_and_enforce_owner`
- `test_notification_mark_as_read_and_not_found_error_mode`
- `test_project_resource_link_routes_require_auth_enforce_owner_and_not_found`

## Migrations Added or Changed

None.

## Validation Results

From `backend/`:

- `uv run --extra dev ruff check app/` ✅
- `uv run --extra dev pyright` ✅
- `uv run --extra dev pytest` ✅ (`225 passed`)

## Deleted or Replaced Shallow Modules

None.

## Remaining Risks

- JWT dev/test secret is still shorter than recommended key length and emits runtime warnings.
- Route-level integration coverage still focuses on key protected endpoints, not every edge path.

## Next Suggested Domain

Cross-domain hardening:

- Tighten JWT secret policy in settings for non-dev environments.
- Expand route-level integration tests for project-resource-link import-building path and additional authorization/error branches.
